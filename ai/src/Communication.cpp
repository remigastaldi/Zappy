
#include "Communication.hpp"

void            Communication::receive_cmd(std::string commande)
{
    cmd = commande;

}


void            Communication::send_cmd()
{

}

std::string     Communication::wait_cmd()
{

    if (answer == "ko")
    if (answer == "dead")
    return (answer);
}
